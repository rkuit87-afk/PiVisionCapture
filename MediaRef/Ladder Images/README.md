# Ladder Screenshot Sources

Place the original TIA screenshots supplied in the 2026-07-14 discussion in
this folder with these names:

1. `01_light_array_manual_bypass.png`
   - Light-array profile selection.
   - Shows manual-active bypass and the light-mask MOVE rungs.
2. `02_thin_board_trim_exception.png`
   - Thin-board exception.
   - Shows the AND operation that removes only the 0.0 trim bit.
3. `03_manual_profile_masks.png`
   - Manipulation-button profile selection.
   - Shows the verified cumulative masks: 3.0=`0x03F9`,
     3.6=`0x03E1`, and 4.2=`0x03C1`.

The screenshots were pasted inline into chat and are not available as files
inside this workspace, so this folder and manifest preserve the intended
location without fabricating image copies. Their transcription is in
[`TIA Ref/PROFILE_BITMASK_MANUAL_CONTROL.md`](../../TIA%20Ref/PROFILE_BITMASK_MANUAL_CONTROL.md).
